---
const avatar = "/profile-picture.png"; // change if you rename your image
const name = "Trevor Tyler Lee";
const tagline =
  "Free, open source LinkTree alternative built with Astro & Tailwind CSS";

const socials = [
  { href: "https://trevortylerlee.com", label: "Website", icon: "🔗" },
  { href: "mailto:you@example.com", label: "Email", icon: "✉️" },
  { href: "https://github.com/gvxsto", label: "GitHub", icon: "🐙" },
  { href: "https://twitter.com", label: "Twitter", icon: "🐦" },
];
---

<section class="treelink-intro" aria-label="Profile">
  <img src={avatar} alt={name} loading="lazy" />

  <h1>{name}</h1>

  <p>{tagline}</p>

  <div class="treelink-social-row" aria-label="Social links">
    {socials.map((s) => (
      <a href={s.href} target="_blank" rel="noopener noreferrer" title={s.label}>
        <span aria-hidden="true">{s.icon}</span>
      </a>
    ))}
  </div>
</section>
